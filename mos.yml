author: mongoose-os
description: Configuration over Bluetooth GATT service support
type: lib
version: 1.0

libs:
  - origin: https://github.com/mongoose-os-libs/bt-common

config_schema:
  - ["bt.config_svc_enable", "b", true, {title: "Enable the config service"}]

tags:
  - bt
  - bluetooth
  - c
  - docs:net:Config over BLE GATT

manifest_version: 2017-09-29
